---

- name: Create a {{ project_name }} virtual machine via vagrant
  hosts: all
  become_method: sudo
  remote_user: ec2-user
  vars_files:
    - host_vars/production.yml

  roles:
    - { role: setup,  tags: fullsetup }
    - { role: deploy, tags: fulldeploy }
