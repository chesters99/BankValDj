---

- name: Create website
  hosts: all
  become_method: sudo
  remote_user: vagrant

  roles:
    - { role: setup,  tags: [ 'full setup',  'all'] }
    - { role: deploy, tags: [ 'full deploy', 'all'] }
