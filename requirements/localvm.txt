-r base.txt # include the base requirements file

django-debug-toolbar==1.4       # debugging toolbar (use IP=127.0.0.1)
django-debug-toolbar-template-timings>=0.6.4 # additional panel for debug toolbar

Werkzeug==0.11.3                # html debugger works with manage.py runserver_plus
django-silk==0.5.6              # html performance analyser access from <host>/silk in browser
Sphinx==1.3.3                   # documentation generator
docutils==0.12                  # required by Sphinx
Jinja2==2.8                     # required by Sphinx
Pygments==2.0.2                 # required by Sphinx for coloring
pylint==1.5.2                   # lint for python
pytest-cov==2.2.0               # coverage reporting for py.test
selenium==2.48.0		        # browser based testing package
pytest==2.8.5                   # python testing module
pytest-django==2.9.1		    # use py.test with Django
pyOpenSSL==0.15.1               # required for runserver_plus on SSL

# the following must be installed with pip2.6 to make Fabric deployment tool work
# fabric
# boto
# paramiko
# fabric==1.8.1 # NOTE: the latest fabric doesnt work with paramiko or pycrpto!!!!
